#summary This page provides some script samples for handling monitoring requests

=== Bash sample script ===

{{{
#!/bin/bash

PORT="9090"

wget -q -O - http://localhost:${PORT}/wlsagent/WlsAgent --post-data=$@ 2> /dev/null | awk -F\| '{ print $2"|"$3 ; exit $1 }'

exit $?

}}}